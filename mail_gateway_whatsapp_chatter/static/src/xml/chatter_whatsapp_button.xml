<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-inherit="mail.Chatter" t-inherit-mode="extension">
        <xpath expr="//button[hasclass('o-mail-Chatter-sendMessage')]" position="before">
            <button
                class="o-mail-Chatter-whatsapp btn text-nowrap me-1"
                type="button"
                t-if="hasGatewayGroup"
                t-att-class="{
                    'btn-primary active': state.whatsappActive,
                    'btn-secondary': !state.whatsappActive,
                    'my-2': !props.compactHeight
                }"
                t-on-click="() => this._toggleWhatsappComposer()"
            >
                <i class="fa fa-whatsapp" role="img" aria-label="WhatsApp" />
                <span> WhatsApp</span>
            </button>
        </xpath>
    </t>
</templates>
